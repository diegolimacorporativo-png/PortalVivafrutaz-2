GOAL:
Migrar TODOS os endpoints /api/products/* de server/routes/routes.ts
para um novo módulo em server/modules/products/,
seguindo o padrão controller → service → repository,
SEM alterar comportamento, SEM alterar response e SEM quebrar nada.

---

STRICT RULES:
- NÃO alterar endpoints (paths, métodos)
- NÃO alterar response (shape, mensagens, status)
- NÃO alterar lógica de negócio
- NÃO alterar validações
- NÃO renomear campos
- NÃO otimizar nada agora
- NÃO remover rotas legacy sem confirmar shadow
- Se qualquer dúvida → PARAR e reportar

---

STEP 1 — DISCOVERY

- Encontrar TODOS os endpoints:

/api/products/*
/api/categories/*
/api/products/next-code
/api/products/check-code
/api/products/check-duplicate
/api/products/safra-alerts
/api/products/price-alerts

- Mapear:
METHOD + PATH → handler inline

---

STEP 2 — MODULE STRUCTURE

Criar:

server/modules/products/

Arquivos:

- products.types.ts
- products.repository.ts
- products.service.ts
- products.controller.ts
- products.routes.ts
- index.ts

---

STEP 3 — TYPES

- Extrair tipos usados (Product, inputs, etc)
- NÃO alterar nomes de campos
- NÃO simplificar

---

STEP 4 — REPOSITORY

Mover acesso a dados:

ANTES:
storage.getProducts()
storage.insertProduct()

DEPOIS:
methods dentro do repository

⚠️ manter comportamento idêntico

---

STEP 5 — SERVICE

Mover TODA lógica de negócio:

- validações
- cálculos
- filtros
- regras existentes

⚠️ NÃO mudar nada
⚠️ NÃO otimizar
⚠️ NÃO alterar fluxo

---

STEP 6 — CONTROLLER

Criar métodos:

- usar asyncHandler OU try/catch padrão existente
- manter mensagens EXATAS

Exemplo:

res.status(400).json({ message: "Código já existe" })

⚠️ NÃO alterar texto

---

STEP 7 — ROUTES

Criar products.routes.ts:

- copiar TODOS endpoints
- manter ordem:

1. rotas fixas primeiro
2. depois rotas com :id

Exemplo:

GET /next-code
GET /check-code
GET /:id

---

STEP 8 — REGISTER MODULE

No:

server/modules/index.ts

Adicionar:

productsModule

Garantir:

app.use('/api/products', router)
antes de registerRoutes()

---

STEP 9 — SHADOW CHECK

Comparar:

- module routes vs routes.ts

Se 100% iguais:
→ marcar legacy como shadowed

Remover inline handlers SOMENTE se:
- módulo já está montado antes
- comportamento confirmado

---

STEP 10 — VALIDATION

Executar:

- npx tsc --noEmit
- npm run check
- npm run check:strict
- npm test

---

STEP 11 — RUNTIME TEST

Testar:

GET /api/products
GET /api/products/:id
GET /api/products/next-code
GET /api/products/check-code
GET /api/products/safra-alerts

Validar:

- status idêntico
- response idêntico

---

STEP 12 — FINAL REPORT

Retornar:

- total de endpoints migrados
- rotas removidas (se houver)
- arquivos criados
- diff (antes vs depois)
- risco final (LOW obrigatório)

---

SUCCESS CRITERIA:

✅ ZERO lógica inline restante
✅ ZERO mudança de comportamento
✅ ZERO mudança de response
✅ módulo products funcionando igual ao legacy