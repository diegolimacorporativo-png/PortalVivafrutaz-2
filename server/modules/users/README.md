# Users Module — `STUB`

## Scope when implemented
- CRUD for internal users (employees), role/tab permissions, MFA toggles

## What to migrate from `server/routes/routes.ts`
- All `/api/users/*`, `/api/team/*`, `/api/permissions/*` routes

## Files to create
Same layered layout as `finance/`: routes / controller / service / repository / validation / types / index.
