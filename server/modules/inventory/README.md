# Inventory Module — `STUB`

## Scope when implemented
- Products, categories, stock movements, waste control, sanitary control

## What to migrate from `server/routes/routes.ts`
- All `/api/products/*`, `/api/categories/*`, `/api/inventory/*`,
  `/api/waste/*`, `/api/sanitary/*` routes

## Files to create
Same layered layout as `finance/`.
