# Sales Module — `STUB`

## Scope when implemented
- Orders, special orders, quotations, contracts, client portal ordering

## What to migrate from `server/routes/routes.ts`
- All `/api/orders/*`, `/api/special-orders/*`, `/api/quotations/*`,
  `/api/contracts/*`, `/api/companies/*` (commercial side)

## Files to create
Same layered layout as `finance/`.
