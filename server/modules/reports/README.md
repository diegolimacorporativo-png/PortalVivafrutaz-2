# Reports Module — `STUB`

## Scope when implemented
- Aggregations, exports (Excel/PDF), executive dashboards, BI queries

## What to migrate from `server/routes/routes.ts`
- All `/api/reports/*`, `/api/dashboard/*`, `/api/executive/*`,
  `/api/exports/*` routes

## Files to create
Same layered layout as `finance/`.
