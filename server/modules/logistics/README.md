# Logistics Module — `STUB`

## Scope when implemented
- Routes, drivers, deliveries, tracking, route optimization

## What to migrate from `server/routes/routes.ts`
- All `/api/logistics/*`, `/api/drivers/*`, `/api/routes/*`, `/api/deliveries/*`
- Services already living under `server/services/logistics/`

## Files to create
Same layered layout as `finance/`.
